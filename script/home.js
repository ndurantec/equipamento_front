document.addEventListener('DOMContentLoaded', function() {
    const link = document.getElementById('contabt');

    // Define o destino do link dinamicamente
    link.href = 'equipamento.html'; // Substitua pelo URL da página desejada
});

document.addEventListener('DOMContentLoaded', function() {
    const link = document.getElementById('fluxobt');

    // Define o destino do link dinamicamente
    link.href = 'manutencao.html'; // Substitua pelo URL da página desejada
});

document.addEventListener('DOMContentLoaded', function() {
    const link = document.getElementById('operacaobt');

    // Define o destino do link dinamicamente
    link.href = 'local.html'; // Substitua pelo URL da página desejada
});